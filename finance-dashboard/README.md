# Finance Dashboard (React + Vite)

This is the initial setup for the Finance Dashboard assignment.

## Step 1 delivered
- Project structure and modular component folders.
- Base responsive dashboard layout.
- Tailwind CSS setup.
- Recharts/Zustand dependencies declared.
- Mock transaction data scaffold.

## Next step
- Replace placeholders with real charts (Recharts) and wire global state with Zustand.

## Run locally
```bash
npm install
npm run dev
```

> Note: In this execution environment, `npm install` is blocked by registry access policy (403), so dependencies could not be installed here.
